import {configureStore, isAction} from "@reduxjs/toolkit"


// step for state management
// submit Action
// handle action in its reducer 
// register here => Redux

export const store = configureStorte({
    reducer:{}
})